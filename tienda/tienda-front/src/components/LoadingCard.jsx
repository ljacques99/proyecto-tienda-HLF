const LoadingCard = ({ className }) => {
  return (
    <div className={`${className} bg-gray-300 animate-pulse`}>
      {/* Puedes agregar más elementos internos si necesitas diferentes áreas de animación */}
    </div>
  );
};

export default LoadingCard;
